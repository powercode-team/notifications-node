{
  "name": "notification",
  "private": true,
  "devDependencies": {
    "@types/node": "^18.0.1",
    "lerna": "^5.1.4",
    "typescript": "^4.7.4"
  },
  "scripts": {
    "publish": "lerna run build && lerna publish"
  }
}
